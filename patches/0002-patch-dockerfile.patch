From d72c19a304678cf5982a4ac2bbc242d8de93e931 Mon Sep 17 00:00:00 2001
From: Mario Attrovio <marioleonardo.attrovio@dentsu.com>
Date: Wed, 14 Jan 2026 13:05:20 +0100
Subject: [PATCH 2/3] patch: dockerfile

---
 .github/workflows/build-push-backend.yml |  64 ++++++++++++++
 Dockerfile                               |  24 +++++-
 patches/0001-backend-working.patch       |  57 +++++++++++++
 scripts/fix.txt                          | 103 +++++++++++++++++++++++
 scripts/update.sh                        |   9 ++
 5 files changed, 255 insertions(+), 2 deletions(-)
 create mode 100644 .github/workflows/build-push-backend.yml
 create mode 100644 patches/0001-backend-working.patch
 create mode 100644 scripts/fix.txt
 create mode 100644 scripts/update.sh

diff --git a/.github/workflows/build-push-backend.yml b/.github/workflows/build-push-backend.yml
new file mode 100644
index 0000000..14eb626
--- /dev/null
+++ b/.github/workflows/build-push-backend.yml
@@ -0,0 +1,64 @@
+name: Build & Push Backend Docker Image
+
+on:
+  push:
+    branches: ["main"]
+
+jobs:
+  build:
+    runs-on: ubuntu-latest
+
+    steps:
+      # Checkout repo
+      - name: Checkout repository
+        uses: actions/checkout@v4
+
+      # Setup Java
+      - name: Set up JDK 21
+        uses: actions/setup-java@v4
+        with:
+          java-version: '21'
+          distribution: 'temurin'
+
+      # Cache Maven dependencies
+      - name: Cache Maven dependencies
+        uses: actions/cache@v4
+        with:
+          path: ~/.m2
+          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
+          restore-keys: ${{ runner.os }}-m2
+
+      # Build with Maven
+      - name: Build with Maven
+        run: mvn clean package -DskipTests
+
+      # Extract version from pom.xml
+      - name: Extract version from pom.xml
+        id: version
+        run: |
+          echo "VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_OUTPUT
+
+      # Setup Docker Buildx
+      - name: Set up Docker Buildx
+        uses: docker/setup-buildx-action@v3
+
+      # Login Docker Hub
+      - name: Log in to Docker Hub
+        uses: docker/login-action@v3
+        with:
+          username: ${{ secrets.DOCKER_USERNAME }}
+          password: ${{ secrets.DOCKER_PASSWORD }}
+
+      # Build and push DEV
+      - name: Build and push DEV Docker image
+        uses: docker/build-push-action@v5
+        with:
+          context: .
+          file: Dockerfile
+          push: true
+          platforms: linux/amd64,linux/arm64
+          cache-from: type=gha
+          cache-to: type=gha,mode=max
+          tags: |
+            ${{ secrets.DOCKER_USERNAME }}/gemimeg-backend:latest-dev-mario
+            ${{ secrets.DOCKER_USERNAME }}/gemimeg-backend:${{ steps.version.outputs.VERSION }}-dev-mario
\ No newline at end of file
diff --git a/Dockerfile b/Dockerfile
index f99f93e..16cd7b8 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -1,4 +1,24 @@
-FROM eclipse-temurin:25-jdk
+# Production-ready Spring Boot Dockerfile
+FROM eclipse-temurin:21-jre
+
+WORKDIR /app
+
+# Create a non-root user for security
+RUN addgroup --system spring && adduser --system spring --ingroup spring
+USER spring:spring
+
+# Copy the pre-built JAR file (built by Maven/GitHub Actions)
 COPY gemimeg-backend/target/gemimeg-backend.jar app.jar
+
+# Expose the port the Spring Boot app runs on
 EXPOSE 10001
-ENTRYPOINT ["java","-jar","app.jar"]
+
+# Health check for Spring Boot Actuator (if enabled)
+HEALTHCHECK --interval=30s --timeout=3s --start-period=60s --retries=3 \
+    CMD wget --no-verbose --tries=1 --spider http://localhost:10001/actuator/health || exit 1
+
+# JVM options optimized for containers and Spring Boot
+ENV JAVA_OPTS="-Xmx512m -Xms256m -XX:+UseG1GC -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -Djava.security.egd=file:/dev/./urandom"
+
+# Run the Spring Boot application
+ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar app.jar"]
diff --git a/patches/0001-backend-working.patch b/patches/0001-backend-working.patch
new file mode 100644
index 0000000..5d19dd5
--- /dev/null
+++ b/patches/0001-backend-working.patch
@@ -0,0 +1,57 @@
+From d95bcce7032d24ab9c34955fb1dbfa198462499e Mon Sep 17 00:00:00 2001
+From: Mario Attrovio <marioleonardo.attrovio@dentsu.com>
+Date: Wed, 7 Jan 2026 17:04:20 +0100
+Subject: [PATCH] backend working
+
+Signed-off-by: Mario Attrovio <marioleonardo.attrovio@dentsu.com>
+
+diff --git a/gemimeg-backend/pom.xml b/gemimeg-backend/pom.xml
+index a79f33e..7512a0e 100644
+--- a/gemimeg-backend/pom.xml
++++ b/gemimeg-backend/pom.xml
+@@ -205,6 +205,11 @@
+       <artifactId>fop</artifactId>
+       <version>${fop.version}</version>
+     </dependency>
++    <dependency>
++      <groupId>net.sf.saxon</groupId>
++      <artifactId>Saxon-HE</artifactId>
++      <version>12.4</version>
++    </dependency>
+     <dependency>
+       <groupId>io.swagger.core.v3</groupId>
+       <artifactId>swagger-core</artifactId>
+diff --git a/gemimeg-backend/src/main/java/de/ptb/common/dcc/service/CalibrationCertificateService.java b/gemimeg-backend/src/main/java/de/ptb/common/dcc/service/CalibrationCertificateService.java
+index fcc938f..c81fc61 100644
+--- a/gemimeg-backend/src/main/java/de/ptb/common/dcc/service/CalibrationCertificateService.java
++++ b/gemimeg-backend/src/main/java/de/ptb/common/dcc/service/CalibrationCertificateService.java
+@@ -72,6 +72,8 @@ import javax.xml.transform.sax.SAXResult;
+ import javax.xml.transform.stream.StreamResult;
+ import javax.xml.transform.stream.StreamSource;
+ import javax.xml.validation.Schema;
++
++import net.sf.saxon.TransformerFactoryImpl;
+ import javax.xml.validation.SchemaFactory;
+ import java.io.ByteArrayOutputStream;
+ import java.io.File;
+@@ -173,7 +175,7 @@ public class CalibrationCertificateService {
+       StreamResult xmlTransformResult = new StreamResult(writer);
+       try (InputStream xslInputStream = this.getClass().getResourceAsStream(DCC_XSL_PATH)) {
+         StreamSource xslResource = new StreamSource(xslInputStream);
+-        TransformerFactory factory = TransformerFactory.newInstance();
++        TransformerFactory factory = new TransformerFactoryImpl();
+         Transformer transformer = factory.newTransformer(xslResource);
+         transformer.transform(xmlResource, xmlTransformResult);
+         return writer.getBuffer().toString();
+@@ -197,7 +199,7 @@ public class CalibrationCertificateService {
+         InputStream xslInputStream = this.getClass().getResourceAsStream(DCC_XSLFO_PDF_PATH)
+     ) {
+       Fop fop = fopFactory.newFop(MimeConstants.MIME_PDF, foUserAgent, out);
+-      TransformerFactory factory = TransformerFactory.newInstance("net.sf.saxon.TransformerFactoryImpl", null);
++      TransformerFactory factory = new TransformerFactoryImpl();
+       Transformer transformer = factory.newTransformer(new StreamSource(xslInputStream));
+       StreamSource xmlSource = new StreamSource(new StringReader(convert(dcc)));
+       Result result = new SAXResult(fop.getDefaultHandler());
+-- 
+2.49.0.windows.1
+
diff --git a/scripts/fix.txt b/scripts/fix.txt
new file mode 100644
index 0000000..72213b4
--- /dev/null
+++ b/scripts/fix.txt
@@ -0,0 +1,103 @@
+REPO: gemimeg-backend
+STYLE: Linux kernel / patch-based
+================================
+
+REMOTES
+-------
+origin   = https://github.com/MeasureStream/gemimeg-backend.git   (MY FORK, WRITE)
+upstream = https://gitlab1.ptb.de/d-ptb/dcc/gemimeg-tool-2/gemimeg-backend.git (OFFICIAL, READ)
+
+
+BRANCHES
+--------
+master        = CLEAN MIRROR OF UPSTREAM
+work/patches  = UPSTREAM + MY PATCHES (DEPLOY FROM HERE)
+(no other long-lived branches)
+
+
+RULES (DO NOT BREAK)
+-------------------
+- NEVER commit on master
+- NEVER merge upstream
+- ONLY use git am / git format-patch
+- PATCH FILES ARE THE SOURCE OF TRUTH
+- origin is the ONLY push target
+
+
+WHERE I DEPLOY FROM
+-------------------
+DEPLOY FROM: work/patches
+(master is NEVER deployed)
+
+
+NORMAL WORKFLOW
+---------------
+
+# Get official updates
+git fetch upstream
+
+# Update master (mirror upstream)
+git checkout master
+git reset --hard upstream/master
+git push --force origin master
+
+# Reapply my patches
+git checkout -B work/patches upstream/master
+git am patches/*.patch
+git push --force-with-lease origin work/patches
+
+
+ADDING A NEW FIX / FEATURE
+-------------------------
+
+git checkout work/patches
+# edit code
+git add .
+git commit -m "patch: <description>"
+
+# Regenerate patches
+git format-patch upstream/master -o patches/ --force
+
+
+PATCH FAILURE RECOVERY
+---------------------
+
+# See failing patch
+git am --show-current-patch=diff
+
+# Fix + continue
+git add <files>
+git am --continue
+
+# Drop obsolete patch
+git am --skip
+
+# Abort everything safely
+git am --abort
+
+
+SANITY CHECK (NO CHANGES)
+------------------------
+
+git apply --check patches/*.patch
+
+
+ONE-LINE STATUS CHECK
+--------------------
+
+git branch
+git status
+git log --oneline --decorate -5
+
+
+MENTAL MODEL
+------------
+
+upstream/master
+        ↓
+      master        (PURE MIRROR)
+        ↓  + patches/*.patch
+  work/patches      (MY REAL CODE / DEPLOY)
+
+
+END
diff --git a/scripts/update.sh b/scripts/update.sh
new file mode 100644
index 0000000..f3d84a6
--- /dev/null
+++ b/scripts/update.sh
@@ -0,0 +1,9 @@
+git fetch upstream
+
+git checkout master
+git reset --hard upstream/master
+git push --force origin master
+
+git checkout -B work/patches upstream/master
+git am patches/*.patch
+git push --force-with-lease origin work/patches
-- 
2.49.0.windows.1

